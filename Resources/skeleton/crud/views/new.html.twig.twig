{% block extends %}
{{ "{% extends 'ItscaroCrudGeneratorBundle::layout.html.twig' %}" }}
{% endblock extends %}

{% block title %}
{{ "{% block title %}" }}
{{ "{{ parent() }} - {{ 'views.new.creation'|trans({'%entity%': '" }}{{ entity }}{{ "'}, 'ItscaroCrudGeneratorBundle') }}" }}
{{ "{% endblock title %}" }}
{% endblock title %}

{% block body %}
{{ "{% block page %}" }}

<h1>{{ "{{ 'views.new.creation'|trans({'%entity%': '" }}{{ entity }}{{ "'}, 'ItscaroCrudGeneratorBundle') }}" }}</h1>

<form class="well" action="{{ "{{ path('"~ route_name_prefix ~"_create') }}" }}" method="post" {{ "{{ form_enctype(form) }}" }}>
    {{ "{{ form_widget(form) }}" }}
    <p>
        <button type="submit" class="btn btn-success">{{ "{{ 'views.new.create'|trans({}, 'ItscaroCrudGeneratorBundle') }}" }}</button>
    </p>
</form>

{% set hide_edit, hide_delete = true, true %}
{% include 'crud/views/others/record_actions.html.twig.twig' %}

{{ "{% endblock page %}" }}
{% endblock body %}
